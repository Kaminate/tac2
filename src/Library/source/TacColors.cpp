#include "TacColors.h"

namespace Tac
{
  const Vector3 Color::red(1,0,0);
  const Vector3 Color::green(0,1,0);
  const Vector3 Color::blue(0,0,1);

  const Vector3 Color::yellow(1, 1, 0);
  const Vector3 Color::orange(1, 0.5f, 0);
  const Vector3 Color::lime(0.5f, 1, 0);
  const Vector3 Color::teal(0, 1, 1);
  const Vector3 Color::purple(0.5f, 0, 1);

  const Vector3 Color::black(0,0,0);
  const Vector3 Color::white(1,1,1);

  const Vector3 Color::darkGrey(0.25f, 0.25f, 0.25f);
  const Vector3 Color::mediumGrey(0.5f, 0.5f, 0.5f);
  const Vector3 Color::lightGrey(0.75f, 0.75f, 0.75f);
}
